name: Bug Report
description: Report a bug encountered while using Stripe Payment
labels: ["bug"]

body:
  - type: markdown
    attributes:
      value: |
        Welcome to the Stripe Payment issue tracker! Before creating an issue, please heed the following:

        1. This tracker should only be used to report bugs and request features / enhancements to Stripe Payment
            - For questions and general support, use the [forum](https://discuss.frappe.io/)
        2. When making a bug report, make sure you provide all required information. The easier it is for
           maintainers to reproduce, the faster it'll be fixed.
        3. If you think you know what the reason for the bug is, share it with us. Maybe put in a PR 😉

  - type: textarea
    id: bug-info
    attributes:
      label: Information about bug
      description: Also tell us, what did you expect to happen?
      placeholder: Please provide as much information as possible.
    validations:
      required: true

  - type: dropdown
    id: module
    attributes:
      label: Module
      description: Select affected module of Stripe Payment.
      multiple: true
      options:
        - checkout
        - subscriptions
        - webhooks
        - customers
        - settlement
        - other
    validations:
      required: true

  - type: textarea
    id: exact-version
    attributes:
      label: Version
      description: Share exact version numbers of the apps you are using.
      placeholder: |
        Frappe version -
        ERPNext version -
        stripe_payment version -
    validations:
      required: true

  - type: dropdown
    id: install-method
    attributes:
      label: Installation method
      options:
        - docker
        - easy-install
        - manual install
        - FrappeCloud
    validations:
      required: false

  - type: textarea
    id: logs
    attributes:
      label: Relevant log output / Stack trace / Full Error Message.
      description: Please copy and paste any relevant log output. This will be automatically formatted.
      render: shell
